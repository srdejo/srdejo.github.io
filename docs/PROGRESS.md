# PROGRESS.md

## Estado actual

El working tree de este repo ya refleja el build Angular del proyecto `portfolio` (publicado vía `npm run publish:portfolio` desde `srdejo-web`), pero **todavía no se hizo commit**. El contenido HTML/CSS/JS vanilla anterior fue reemplazado localmente.

## Próximo paso recomendado

1. Revisar el sitio migrado (`ng serve portfolio` desde `srdejo-web`, o abrir los archivos generados acá) antes de publicar.
2. Con el visto bueno del usuario, hacer `git add`/`git commit`/`git push` desde este repo.
3. Verificar en `https://srdejo.github.io/` que GitHub Pages sirve el nuevo contenido correctamente tras el push.
